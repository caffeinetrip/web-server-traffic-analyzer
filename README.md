# Advanced Traffic Analyzer

Тестове завдання на позицію **Junior Backend Developer**.  
Скрипт аналізує логи веб-сервера, підтримує фільтрацію, агрегацію даних та формує структурований звіт у консоль.

---

## 📌 Опис рішення

`advanced_traffic_analyzer.py` — це CLI-скрипт для аналізу access.log файлів у форматі:


Скрипт:
- читає лог построчно (підходить для великих файлів),
- валідовує дані,
- застосовує фільтри,
- рахує статистику,
- виводить зрозумілий звіт.

Некоректні рядки не ламають виконання — вони пропускаються з warning.

---

## 🧩 Функціональність

Підтримуються фільтри:
- HTTP-метод (`--method`)
- статус код або діапазон (`--status 400-499`)
- часовий інтервал (`--start`, `--end`)
- кількість IP у топі (`--top`)

Збирається статистика:
- загальна кількість запитів
- кількість унікальних IP
- топ-N IP за активністю
- унікальні IP за останні 24 години
- розподіл HTTP-методів (%)
- топ-5 URL
- загальний обʼєм переданих даних
- кількість помилок 4xx / 5xx
- середній розмір відповіді для 2xx

---

## ▶️ Приклади запуску

### Базовий запуск
```bash
python advanced_traffic_analyzer.py sample_access.log
